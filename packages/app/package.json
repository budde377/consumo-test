{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "apollo-boost": "^0.3.1",
    "graphql": "^14.3.0",
    "graphql-tag": "^2.10.1",
    "react": "^16.8.6",
    "react-apollo": "^2.5.5",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "styled-components": "^4.2.0",
    "typeface-open-sans": "0.0.54",
    "typeface-roboto": "0.0.54"
  },
  "scripts": {
    "start": "REACT_APP_API_URL=http://localhost:4000 react-scripts start",
    "build:js": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:docker": "docker build -t ${API_IMAGE:-budde377/consuno-app} -f docker/Dockerfile .",
    "deploy:docker": "docker push ${API_IMAGE:-budde377/consuno-app}",
    "deploy": "npn run deploy:docker",
    "build": "npm run build:js && npm run build:docker"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
